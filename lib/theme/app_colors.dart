import 'package:flutter/material.dart';

class AppColors {
  // Main TubiLearn colors
  static const Color primary = Color(0xFF6C63B5);
  static const Color primaryDark = Color(0xFF514A96);

  // Tubi-inspired accents
  static const Color green = Color(0xFF79A86B);
  static const Color yellow = Color(0xFFF4C95D);

  // Backgrounds
  static const Color background = Color(0xFFF9F7FF);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF2D2D35);
  static const Color textSecondary = Color(0xFF777582);

  // Utility
  static const Color border = Color(0xFFE8E5F0);
}